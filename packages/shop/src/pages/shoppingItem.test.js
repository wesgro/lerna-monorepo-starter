describe('Layout component', () => {
  const mockProps = {
    pathContext: {
      data: {
        title: 'mock title',
        content: 'mock content',
        href: 'google.com',
      },
    },
  }

  it('should render', () => {
    expect(true).toBe(true)
  })
})
